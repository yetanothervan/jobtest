const base = '/api/bigdata/';
const _inner_routes = {
    getdata: `${base}getdata`,
    getnode: `${base}getnode`,
    reset: `${base}reset`,
    apply: `${base}apply`,
}
export default _inner_routes;